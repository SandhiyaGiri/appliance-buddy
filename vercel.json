{
  "version": 2,
  "builds": [
    {
      "src": "appliance-buddy/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    },
    {
      "src": "backend/api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/backend/api/index.ts"
    },
    {
      "src": "/(.*)",
      "dest": "/appliance-buddy/$1"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}